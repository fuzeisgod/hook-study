/**
 * React 状态钩子，用于追踪对象型值。
 * 返回一个对象的当前状态和控制对象的方法的数组
 * 方法列表：
 *  get：取某一key对应的值
 *  set：设置某一key的值
 *  remove：移除某一key
 *  reset：重置对象
 */

import { useState } from 'react'


const useMap = (initialMap: any = {}) => {

}

export default useMap;